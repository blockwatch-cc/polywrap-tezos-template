#import { Mutation, Connection, CallContractMethodConfirmationResponse} into Tezos from "w3://ens/tezos.web3api.eth"

type Mutation {
    increment(
        address: String!
        value: UInt32!
        connection: Tezos_Connection
    ): Tezos_CallContractMethodConfirmationResponse!

    decrement(
        address: String!
        value: UInt32!
        connection: Tezos_Connection
    ): Tezos_CallContractMethodConfirmationResponse!
}

